# v5.3.3 评测边界

- 本包验证持续在线会话中的初始检索、历史追加、状态连续性和后续复用；后续题不只读取预置历史。
- 未冻结能力不进入正式分：精准遗忘只保留在 experimental runtime。
- 功能算法尚未冻结的能力不能由本版主分外推证明；系统执行效果、麒麟适配和物理数据擦除需要另行测试。
- Agent 不得访问 Judge Release。runner 的签名和链摘要证明 trace 来自持续运行流程，但权限隔离仍需由测试环境负责。
